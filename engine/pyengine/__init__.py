"""tm-opt pyengine — Triton-first Inkling inference engine (decision D5).
Bring-up is driven by PROGRESS.md; shapes come from tmopt config (verified
against the checkpoint by ./build/verify_config). CUDA ports of these kernels
are Stage-3 experiments, not bring-up work."""
__version__ = "0.0.1"
