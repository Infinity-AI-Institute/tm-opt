"""Triton kernel: moe_gate — implemented by the build loop per its B-item.
Contract + shapes in the corresponding model.py call site; torch reference
implementation lives beside it in tests until the kernel replaces it."""
#TODO
