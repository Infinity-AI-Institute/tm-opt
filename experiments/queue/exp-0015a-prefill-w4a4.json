{
  "experiment_id": "exp-0015a-prefill-w4a4",
  "commit": "09ae5faeb5523ff4bb39ca3750006598643134ff",
  "workload": "decode_heavy",
  "label": "Native W4A4 prefill",
  "mechanism": "prefill routed experts on CUTLASS-DSL FP4xFP4 grouped MMA + Triton NVFP4 activation quant (the reference's own recipe); decode stays W4A16",
  "hypothesis": "The accepted cycle is prefill-term-dominated (exp-0013 block: 91.4 s/cohort = ~61 s serial prefill + ~30.4 s decode) and prefill is GPU-bound in the grouped W4A16 Triton GEMM whose in-register dequant chain is the ALU floor exp-0010 identified (~2.5 cvt-class ops/element, ~5.7x off roofline). Replacing the prefill-path expert GEMMs with native FP4xFP4 tensor-core MMA — the exp-0015 groundwork's exact-validated CUTE DSL grouped blockscaled kernel (tiles 128,128,128) fed by the bitwise-validated swizzled-scale activation-quant kernel, per-expert alpha=input_scale*scale2 in-kernel, expert groups 128-row-padded (zero extra tiles; SFA layout degenerates to the validated global swizzle) — cuts the measured per-layer MoE wall at T=736 from 20.29 ms to 4.11 ms (4.9x, t_moe_w4a4 arm f on real layer-3 experts). W4A4 on these layers is the reference's own ModelOpt recipe (vLLM quantizes activations with input_scale=input_amax/(448*6); the D11 goldens/D13 envelope come from that path), so precision moves TOWARD the goldens: local D13 TF gate GREEN at agree 0.9475 (floor 0.945), delta_mean 0.043856 vs bar 0.067851 (prior engine 0.05065), delta_max 0.396472. Correctness: every hit expert matches a fp32-out _scaled_mm arbiter on identical operands bitwise up to single-element 1-bf16-ulp accumulation ties (incl. a 156-row two-M-tile group); output bitwise-deterministic across reruns; decode paths (incl. the CUDA-graphed step) untouched. First request pays ~32 s one-time (8 DSL compiles + 63 layer scale repacks; +~0.9 GB/layer resident blocked scales).",
  "predicted_delta": "+40-65% timeboxed decode_heavy tok/s (126-148 vs best 89.7, merge bar 89.97): cycle model 8192/(30.4 + prefill_term) with the ~75-80% expert-GEMM share of the 0.95 s/seq prefill cut 4.9x -> prefill/seq 0.34-0.39 s -> prefill term 22-25 s -> 148-156 ceiling, discounted for the first-cohort compile storm and eager silu/epilogue residuals. prefill_heavy gains ride the same path (remeasure later as a point row).",
  "baseline_id": "vllm_mtp_off",
  "serve_cmd": "python -m engine.pyengine.server --port 8200"
}
